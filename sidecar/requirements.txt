# AI Visor Python 사이드카 의존성
#
# 필수 — PPTX 텍스트·발표자 노트 추출:
python-pptx>=0.6.23
#
# PDF 텍스트 추출 + (PPTX) 슬라이드 이미지 변환(PDF→PNG).
# 없으면 PDF는 열 수 없고 PPTX는 텍스트 모드로 폴백한다(크래시 없음).
PyMuPDF>=1.24.0
#
# 선택 — DOCX(Word) 본문·표 추출. 없으면 DOCX만 "사용 불가"로 안내하고 다른 타입은 정상.
python-docx>=1.1.0
