# AI Visor 환경 변수 예시
#
# ⚠ API 키는 이 파일에도, .env에도, 코드/번들 어디에도 넣지 않는다 (CLAUDE.md R7).
#   Anthropic API 키는 앱 실행 후 사용자가 런타임에 직접 입력한다.
#   난독화·암호화 동봉도 금지 (추출 가능하므로).

# (예약, +1) Python 사이드카 로컬 포트 — 기본값을 바꿀 때만 설정
# SIDECAR_PORT=8765
