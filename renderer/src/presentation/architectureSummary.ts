/**
 * 메타 장치 (+2 ④): 발표 컨텍스트에 주입하는 "이 앱 자신"의 아키텍처 요약.
 *
 * 발표 컨트롤러가 본체에 넣는 Message에 이 요약을 포함시켜,
 * AI가 자기 구조를 실제 사실에 근거해 설명·답변하게 한다.
 * 내용은 CLAUDE.md·ARCHITECTURE.md의 압축본 — 문서가 바뀌면 함께 갱신한다.
 */

export const APP_ARCHITECTURE_SUMMARY = `이 앱(AI Visor)은 감정을 읽고 기억을 쌓으며 대화하는 로컬 우선 데스크톱 AI 동반자다. 스택: Electron + Next.js 렌더러 + Python 사이드카(예정) + 로컬 SQLite(WAL).
- 입력 통일(R1): 채팅·음성·발표 등 모든 입력은 Message 객체(source, text, timestamp)로 정규화되어 본체에 들어간다. 본체는 source로 동작을 분기하지 않는다.
- 단일 출력 스트림(R2): LLM 출력은 하나의 이벤트 스트림으로 흐르고 자막·표정·TTS가 각자 구독한다.
- 발표 격리(R3): 본체(대화 엔진)는 발표를 모른다. 지금 이 발표도 본체 바깥의 발표 컨트롤러가 본체를 호출해서 진행하는 것이다 — 그래서 발표 중에도 감정·기억이 평소처럼 작동한다.
- 도구 게이트(R4): 모든 도구는 단일 레지스트리에 risk 태그(safe/caution/dangerous)로 등록되고, dangerous는 실행 직전 사용자 승인 게이트를 코드로 강제한다. 모든 호출은 감사 로그에 남는다.
- 감정 분리(R5): 감정·유대(affection)는 어투에만 영향을 주고 작업 실행 결정에는 개입하지 않는다. risk 태그는 감정과 무관한 코드 상수다.
- 감정(VAD): Valence·Arousal·Dominance 3축을 답변과 같은 LLM 호출에서 마커(<vad>)로 추론하고, 스무딩(가중평균)과 유휴 감쇠를 거쳐 2D 표정과 어투에 반영한다.
- 기억(R6): 단기(세션 캐시) → 종료 시 요약 + 사실(키-값) 추출 → 로컬 SQLite 영속. N턴마다 스냅샷으로 크래시에 대비한다. 클라우드 전송 없음.
- 유대(affection): 완료된 대화 턴마다 조금씩 쌓이고 어투 3단계(초면→친근→편안)로만 반영된다.
- 모델 라우팅: 가벼운 발화는 빠른 모델, 작업·긴 발화는 고성능 모델로 규칙 기반 라우팅한다.
- API 키(R7): 키는 코드·번들에 없고 사용자가 런타임에 입력한다.`
